# Фронт приложение для работы с контрактами

```shell
yarn
yarn hardhat compile
yarn hardhat node
```

## Hardhat

```shell
yarn hardhat accounts
yarn hardhat clean
yarn hardhat compile
yarn hardhat deploy
yarn hardhat help
yarn hardhat node
yarn hardhat test
```
